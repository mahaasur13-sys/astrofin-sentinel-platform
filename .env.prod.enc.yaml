# Placeholder SOPS-encrypted stub.
# In production, regenerate with:
#   sops --encrypt --age age1real... .env.prod.example > .env.prod.enc.yaml
#
# This stub is committed so the path-encryption regex in .sops.yaml is exercised
# in CI; rotation will replace it with the real encrypted blob once the operator
# provides an age recipient key (see docs/SECURITY.md §SOPS-Key-Rotation).
sops_placeholder: true
last_rotated: 2026-07-08
rotation_owner: security@astrofin.local
data: ENC[AES256_GCM,data=placeholderbase64==,tag=placeholder==,type=placeholder]
EOF
