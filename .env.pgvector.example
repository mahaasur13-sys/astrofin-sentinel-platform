# Local pgvector dev defaults (DO NOT use these in prod).
# Copy to .env.pgvector and set POSTGRES_PASSWORD before `docker compose up`.
PGPORT=5432
PGDATABASE=astrofin
PGUSER=astrofin
# Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=changeme
