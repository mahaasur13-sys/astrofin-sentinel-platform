# Техдолг

Живой реестр техдолга. Формат: `[ ]` — открыто, `[x]` — закрыто.
У каждой записи — оценка, срок, владелец.

---

## KI-127 — auth env rename

- [ ] Удалить shim `REQUIRE_AUTH` в `core/auth.py` после того, как все
      вызовы в репозитории и в прод-конфигах (`docker-compose.yml`,
      systemd unit, deploy overlays) перейдут на `API_KEY_AUTH_DISABLED`.
      **Оценка:** 0.5 дня (после 1–2 спринтов эксплуатации нового имени).
      **Срок:** конец спринта, в котором истечёт deprecation window.
- [ ] Закрыть CodeRabbit review-треды после зелёного Quality Gate.
      **Оценка:** 15 мин. **Срок:** в день мержа #176.

---

## KI-128 — Workflow triggers (временный workaround)

- [ ] Временный workaround для GitHub workflow triggers — добавлен `feat/**`
      в branch-паттерны `ci.yml`, `ci.security.yml`, `quality-gate.yml`,
      `compose-check.yml`. После мержа и стабилизации CI на `feat/**`
      оценить, нужно ли оставить `feat/**` в триггерах на постоянной
      основе или ограничить список.
      **Оценка:** 0.5 дня (аудит после 1–2 недель эксплуатации).
      **Срок:** середина следующего спринта.
- [ ] Удалить дублирующиеся/протухшие feature-ветки в remote после мержа
      PR #176, чтобы не накапливать мёртвые refs.
      **Оценка:** 15 мин. **Срок:** в день мержа #176.
- [ ] Закрыть тред KI-128 в CodeRabbit после зелёного Quality Gate на
      `fix/ki-128-workflow-triggers`.
      **Оценка:** 15 мин. **Срок:** в день мержа KI-128.

---

## Шаблон новой записи

```markdown
## <KI-ID> — <короткое название>

- [ ] <задача>
      **Оценка:** <X дней>
      **Срок:** <дата / событие>
      **Контекст:** <ссылка на PR / задачу, почему появилось>
```
