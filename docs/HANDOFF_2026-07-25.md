# Handoff — 2026-07-25 (Audit Closed → Sprint A)

> Скопируй в новый чат для продолжения работы без потери контекста.

---

## Состояние проекта

- **Repo:** `mahaasur13-sys/astrofin-sentinel-platform`
- **Branch:** `master` @ `c1b7e8d1` (на Zo) / `da2f8cf9` (финальный push)
- **Tag:** `v2026.07.25-audit-closed`
- **CI:** 9 workflows, 0 открытых PR
- **Локальная машина:** `~/Projects/astrofin-sentinel-platform`, виртуалка `.venv`

## Что сделано (сессия 2026-07-25)

### Аудит — все 3 волны закрыты

| PR | Задача |
|----|--------|
| #268 | Bandit un-masking, SQL injection fix, 35 outdated packages |
| #269 | Roma agent dedup (7 root copies → archived) |
| #270 | Unified deps Part 1 |
| #272 | AUDIT_REPORT → docs/audits/ |
| #273 | test_api_auth isolation |
| #274 | F401 per-file-ignores |
| #275 | Ruff unlift meta_rl/trading |
| #276 | callbacks.py 1032→5 модулей |
| #278 | PART2: CI на pyproject [dev], requirements-dev.txt удалён |
| #279 | Dependabot: 16 production deps |
| #280 | CI 17→9 workflows |

### 10/10 findings закрыты

### Sprint A: Production DB + Observability (начат)

| Задача | Статус | Файлы |
|--------|--------|-------|
| A1: market_data hypertable | ✅ done | `migrations/0008_market_data_hypertable.sql` (57 строк), `db/models.py` |
| A2: WAL-G backups | ⚠️ не начат | RUNBOOK.md всё ещё говорит "not yet configured (issue #131)" |
| A3: SLO/SLI recording rules | ⚠️ не начат | SLO.md есть (3 SLO), Prometheus recording rules — нет |
| A4: Grafana dashboards | ⚠️ базовые есть | 8 JSON в deploy/, не подключены к метрикам |
| A5: PII scrubber tests | ✅ done | `tests/observability/test_logging_scrubber.py` (19 тестов, 16 pass) |
| A6: Dependabot cleanup | ✅ done | 0 open PRs |

### Баги в имплементации (найдены через A5)

1. `core/logging_utils.py::_scrub_string` — падает на не-строках (int, None)
2. `core/logging_utils.py::scrub_pii` — падает на не-словарях
3. `_scrub_string` не ловит JWT в формате `Bearer eyJ...` (регекс слишком узкий)

## Открытые проблемы

1. **test_api_auth flaky** — возвращает 200 вместо 401/403 (PR #273 изоляцию добавил, но не полностью)
2. **WAL-G** — RUNBOOK §DR ссылается на issue #131, конфига нет
3. **SLO recording rules** — PromQL определён, но rules файла нет
4. **Grafana dashboards** — JSON есть, но не интегрированы с текущими метриками
5. **uv.lock** — обновлён на Zo, на локальной машине может потребоваться `uv sync`

## Файлы для внимания (новые в этой сессии)

```
docs/
├── HANDOFF_2026-07-25.md          ← этот файл
├── audits/
│   ├── audit-report-step2.md
│   └── audit-report-2026-07-25.md
├── CI_CONSOLIDATION_PLAN.md
├── RELEASE_NOTES_v2026.07.25.md
├── PRODUCTION_BACKLOG.md
├── SPRINT_1.md + SPRINT_1_ISSUES.md
├── SPRINT_2.md
├── MOSCOW_PRIORITIZATION.md
└── GITHUB_PROJECT_SETUP.md

migrations/
└── 0008_market_data_hypertable.sql

tests/observability/
└── test_logging_scrubber.py

PART2_workflows.patch               ← можно удалить (уже применён)
CI_CONSOLIDATION.patch              ← можно удалить (уже применён)
```

## Промпт для нового чата

```
Ты — Senior Architect на проекте AstroFin Sentinel V5.

Контекст: только что завершён трёх-волновой аудит (10/10 findings, 13 PR #268–#280),
CI сокращён с 17 до 9 workflows, callbacks.py разбит на 5 модулей,
зависимости унифицированы в pyproject.toml [dev].
Состояние: master @ c1b7e8d1, tag v2026.07.25-audit-closed.

Начат Sprint A (Production DB + Observability):
- A1 (market_data hypertable) — ✅ миграция создана
- A2 (WAL-G backups) — не начат
- A3 (SLO/SLI recording rules) — не начат
- A4 (Grafana dashboards) — JSON есть, не подключены
- A5 (PII scrubber tests) — 16/19 pass, 3 бага в имплементации
- A6 (dependabot) — ✅ 0 open PRs

AGENTS.md и SOUL.md актуальны (прочитай их).

Первая задача: исправить 3 бага в core/logging_utils.py (non-string guard в _scrub_string,
non-dict guard в scrub_pii), затем Sprint A task A2 — WAL-G backup конфигурация.
```

## Быстрый старт на локальной машине

```bash
cd ~/Projects/astrofin-sentinel-platform
git pull origin master
source .venv/bin/activate
python tools/healthcheck.py
python -m pytest tests/observability/test_logging_scrubber.py -v
```
