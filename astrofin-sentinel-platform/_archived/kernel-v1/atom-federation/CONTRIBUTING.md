# Contributing to ATOM Federation OS

Thanks for contributing to ATOM Federation OS.

## Setup

```bash
pip install -r requirements-dev.txt
pre-commit install
```

## Hard Constraints (RL-022)

## Determinism is mandatory.

See `DETERMINISM_FIX_PLAN.md` and `ATOM-META-RL-022-PRODUCTION-FINALIZATION.md` for context.

C1-C10 constraints must be