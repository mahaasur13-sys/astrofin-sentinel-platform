# =============================================================================
# ATOM Federation OS — cluster node runtime dependencies
#
# This file pins only what `cluster/node/*` actually imports at runtime.
# Everything else (rich, click, hypothesis, etc.) lives in the project-root
# `requirements.txt` / `pyproject.toml [project.optional-dependencies]` and
# is intentionally NOT pulled into the node image — the node image must stay
# lean for fast cold-start in 3-node cluster compose deployments.
#
# Pinned versions (verified 2026-06-19 against requirements.lock + venv):
#   • grpcio 1.80.x     — last series that ships a CPython 3.12 wheel and is
#                          ABI-compatible with protobuf 6.33.x.
#   • protobuf 6.33.x   — paired with grpcio 1.80.x; older 5.x line also
#                          works, but we standardize on 6.x for symmetry with
#                          AsurDev/acos.
#
# Regenerate after a deliberate upgrade:
#   pip freeze | grep -E "^(grpcio|protobuf)" > cluster/node/requirements.txt
# =============================================================================

grpcio==1.80.0
protobuf==6.33.6