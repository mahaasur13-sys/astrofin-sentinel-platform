# =============================================================================
# ACOS (Autonomous Constrained Optimization System) — runtime dependencies
#
# ACOS itself only uses Python stdlib. The integration surfaces it loads at
# runtime (l9_ebl, ete, l10_self_healing, constraint_compiler) and the
# observability / CLI paths also stay stdlib-only at the moment.
#
# This file is intentionally minimal so the ACOS image is a thin python:3.12-slim
# layer. Add a pin here ONLY when a new third-party dep is introduced in
# acos/ or acos_cli.py.
# =============================================================================

# (no runtime deps today; file kept for future additions and explicit pin point)
