apiVersion: v1
kind: Service
metadata:
  name: astrofin-sentinel-web
  namespace: astrofin-sentinel
  labels:
    app.kubernetes.io/name: astrofin-sentinel-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: astrofin-sentinel-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP