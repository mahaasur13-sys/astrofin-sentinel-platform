__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
.venv/
build/
dist/
*.bak
graphify-out/inferred_clean.*
graphify-out/cache/
ab_*

# local secrets template (never commit)
.env.secrets
.env
# decrypted secrets (regenerated by CI / `sops -d`)
config/secrets.yaml
# SOPS age private key (never commit — use SOPS_AGE_KEY_FILE in CI)
age.key

# graphify-out runtime artifacts (do not commit)
graphify-out/inferred_clean_filtered.jsonl
graphify-out/_ad_hoc_ov_check*.jsonl
graphify-out/*.filtered.jsonl

# audit_repo — локальная копия репо для аудита (не для коммита)
audit_repo/

# pytest-hypothesis database (do not commit)
.hypothesis/
.github/workflows/decrypt-secrets.yml
