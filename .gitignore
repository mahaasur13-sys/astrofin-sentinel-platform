__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
.venv/
build/
dist/
*.bak
graphify-out/inferred_clean.*
graphify-out/cache/
ab_*

# local secrets template (never commit)
.env.secrets
.env

# graphify-out runtime artifacts (do not commit)
graphify-out/inferred_clean_filtered.jsonl
graphify-out/_ad_hoc_ov_check*.jsonl
graphify-out/*.filtered.jsonl

# audit_repo - local copy of repo for audit (do not commit)
audit_repo/

# pytest-hypothesis database (do not commit)
.hypothesis/

# Secrets (decrypted locally, never commit)
config/secrets.yaml
config/secrets.secret.yaml
age.key
.south-secrets/
venv/

# Python venvs (NEVER commit)
venv/
.venv/
env/
ENV/

# IDE
.idea/
.vscode/

# Local config with secrets
.env
.env.local

# Coverage artifacts (do not commit)
*.coverage
.coverage
coverage.xml
unified-platform/.coverage
htmlcov/
backtest/results.db

# JWT signing keys — private keys are NEVER committed.
# Public keys (jwt_public.pem) are intentionally committed so that
# verifiers (FastAPI, Flask) can validate tokens without secret distribution.
keys/jwt_private.pem
keys/jwt_private*.pem
keys/*.key
core/history.db

# Legacy snapshots and archives (cleaned 2026-07-09)
as/
audit_repo/
audit-astro/
audit-astrofin/
backups/
push_backup/
archive/
_sbs_old/
_pr_logs/
finish-pr-179.sh
finish-pr-179.sh
