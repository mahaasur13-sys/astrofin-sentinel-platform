__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
.venv/
build/
dist/
*.bak
graphify-out/inferred_clean.*
graphify-out/cache/
ab_*

# local secrets template (never commit)
.env.secrets
.env

# graphify-out runtime artifacts (do not commit)
graphify-out/inferred_clean_filtered.jsonl
graphify-out/_ad_hoc_ov_check*.jsonl
graphify-out/*.filtered.jsonl

# audit_repo - local copy of repo for audit (do not commit)
audit_repo/

# pytest-hypothesis database (do not commit)
.hypothesis/

# Secrets (decrypted locally, never commit)
config/secrets.yaml
config/secrets.secret.yaml
age.key
.south-secrets/
venv/

# Python venvs (NEVER commit)
venv/
.venv/
env/
ENV/

# IDE
.idea/
.vscode/

# Local config with secrets
.env
.env.local

# Coverage artifacts (do not commit)
*.coverage
.coverage
coverage.xml
unified-platform/.coverage
htmlcov/
