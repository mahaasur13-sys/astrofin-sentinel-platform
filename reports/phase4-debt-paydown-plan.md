# Фаза 4 — план: техдолг-paydown

## Скоуп 7 провалов из CI (run #28437207086)

| # | Задача | Затронутые пакеты | Приоритет | Стратегия |
|---|--------|-------------------|-----------|-----------|
| 1 | Архитектурный линтер | `archive/tools/thompson_cli.py` | **P0** | Исправить импорты, перенести в `tools/` (см. ARCHITECTURE.md) |
| 2 | Sub-pkg тесты: `bridge/roma-execution-bridge` | `bridge/` | **P0** | Перенести тесты рядом, conftest, smoke-тест |
| 3 | Sub-pkg тесты: `kernel/atom-federation-os` | `kernel/` | **P0** | То же: pytest, conftest, smoke |
| 4 | Meta-RL persistence | `meta_rl/persistence.py` | **P0** | Разобрать import-ошибки, поднять тест, починить API |
| 5 | Data-room валидация | `data_room/` | **P0** | Понять формат, поправить валидатор |
| 6 | Hard-rule нарушение в `thompson_cli.py` | (часть #1) | **P0** | См. #1 |
| 7 | Привести CI к зелёному (все 7 джоб) | — | **P0** | Цель: ✅ на main |

## План

### Шаг 1: разведка (дешево)
- Прочитать ARCHITECTURE.md (правила импортов).
- Прогнать `python scripts/architecture_linter.py` локально, увидеть что падает.
- Скачать логи CI run #28437207086 по каждому из 7 джоб.
- Просмотреть содержимое `bridge/`, `kernel/`, `meta_rl/`, `data_room/`.

### Шаг 2: фикс архитектурного линтера (P0)
- Если hard-rule нарушается в `archive/tools/thompson_cli.py` — переместить/переименовать согласно ARCHITECTURE.md.
- Если линтер сам ошибается — поправить правила.
- Проверить локально → 0 hard-violations.

### Шаг 3: фикс sub-пакетов (P0)
- `bridge/`: добавить `tests/`, `conftest.py`, `pytest.ini`/`pyproject.toml` секцию.
- `kernel/`: то же.
- Минимум по одному smoke-тесту в каждом.

### Шаг 4: фикс meta-rl/persistence (P0)
- Разобрать import-цепочку.
- Восстановить API, добавить тест.

### Шаг 5: фикс data-room (P0)
- Понять схему, валидировать по факту, тест.

### Шаг 6: гонка CI
- Push, дождаться прогона, зелёный.

## Стратегия

- **Без breaking changes в main коде**: только импорты/имена/тесты, никаких изменений продуктовой логики.
- **Минимальные тесты**: 1-3 smoke-теста на пакет, достаточно для CI pass.
- **Commit-per-fix**: 5 коммитов, по одному на каждый шаг, легко откатывать.
- **Если задача уходит в большой рефактор** — остановиться и спросить пользователя.

## Out of scope

- Покрытие >80% (это не debt).
- Документация (Фаза 5).
- Performance-оптимизации.
- Реальный re-design meta-rl.
